<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8' />
    <title>Health Payer Processor</title>
    <style>
      td{padding: 8px;}
    </style>
  </head>
  <body>
    <h1><%= @title %></h1>
    <table border="1">
        <% @arrays.each do |id, fname, lname, pos| %>
          <tr>
            <td><%= id %></td>
            <td><%= fname %></td>
            <td><%= lname %></td>
            <td><%= pos %></td>
          </tr>
        <% end %>
    </table>
      
  </body>
</html>