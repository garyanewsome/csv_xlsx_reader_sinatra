<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8' />
    <title>Health Payer Processor</title>
    <style>
      td{padding: 8px;}
    </style>
  </head>
  <body>
    <h1><%= @title %></h1>
    <hr>
    <p>This page is unstyled by design / default</p>
    <hr>
    <h2>Proposed Dropdown List</h2>
    <select>
      <option value="payer1">Payer #1</option>
      <option value="payer2">Payer #2</option>
      <option value="payer3">Payer #3</option>
      <option value="payer4">Payer #4</option>
      <option value="payer5">Payer #5</option>
    </select>
    
    <h2>File Upload Test</h2>
    <form method="post" enctype="multipart/form-data">
      <input type="file" name="uploadfile"><br>
      <input type="submit" value="Upload">
    </form>

    <h2>Extraction Test</h2>
    <h3>CSV Table</h3>
    <table border="1">
        <% @csv_arrays.each do |id, fname, lname, pos| %>
          <tr>
            <td><%= id %></td>
            <td><%= fname %></td>
            <td><%= lname %></td>
            <td><%= pos %></td>
          </tr>
        <% end %>
    </table>

    <h3>XLSX Table</h3>
    <table border="1">
        <% @xlsx_arrays.each do |id, fname, lname, pos| %>
          <tr>
            <td><%= id %></td>
            <td><%= fname %></td>
            <td><%= lname %></td>
            <td><%= pos %></td>
          </tr>
        <% end %>
    </table>

  </body>
</html>