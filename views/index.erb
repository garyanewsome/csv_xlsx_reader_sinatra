<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8' />
    <title>Health Payer Processor</title>
  </head>
  <body>
    <h1><%= @title %></h1>
    <p>Please check your terminal...</p>
    <ul>
        <% @array.each do |id, fname, lname, pos| %>
          <li>
            <%= id %> <%= fname %> <%= lname %> <%= pos %>
          </li>
        <% end %>
    </ul>
      
  </body>
</html>